import type { CSSProperties } from "react";

// Shared style constant used across SettingsModal's per-tab components.
export const inputStyle: CSSProperties = {
  width: '100%',
  padding: '6px 10px',
  background: 'var(--term-bg)',
  border: '1px solid var(--term-border)',
  borderRadius: '4px',
  color: 'var(--term-fg)',
  fontSize: '13px'
};
