---
iteration: 7
date: 2026-02-22
milestone: E1-S1 — Sprint 1 complete
tasks_completed: [TASK-20260222-013, TASK-20260222-014]
tasks_created: []
---

# Iteration 7 Run Log

## Objective
Complete Sprint 1: notes_text plain-text column (TASK-013) and expanded chat tools (TASK-014).

---

## TASK-20260222-013 — DB: notes_text plain-text column (DONE)

### Discovery
Migration "v5" was already used for `api_source` column. Used **v6** instead.

### Implementation

**`store/models.go`**:
- Added `NotesText string \`json:"notes_text,omitempty"\`` to `Item` struct

**`store/schema.sql`**:
- Added `notes_text TEXT DEFAULT ''` column to `todos`
- Updated FTS5 virtual table: `notes_text` indexed instead of `notes_md`
- Updated triggers `todos_ai`, `todos_au`, `todos_ad` to use `notes_text`

**`store/store.go`**:
- Added imports: `"fmt"`, `"regexp"`
- Added package-level regex vars: `reHTMLBR`, `reHTMLBlock`, `reHTMLTag`, `reMultiNL`
- Added `stripHTML(s string) string` — pure Go, no CGo; handles `<br>`, block-close tags, all HTML tags, common HTML entities; preserves paragraph breaks as `\n\n`
- Bumped `currentSchemaVersion` to 6
- Migration v6: multi-step with special handling — ALTER TABLE IF NOT EXISTS guard via `pragma_table_info`; Go-side backfill (`UPDATE todos SET notes_text = ?`); DROP + CREATE FTS5 table with `notes_text`; DROP + CREATE triggers; `INSERT INTO todos_fts(todos_fts) VALUES('rebuild')`
- `CreateItem`: INSERT includes `notes_text = stripHTML(t.NotesMD)`
- `UpdateItem`: UPDATE includes `notes_text = stripHTML(t.NotesMD)`
- `GetItem`, `GetBackrefs`, `Search`, `GetInboxItems`: SELECT + Scan include `notes_text`
- Removed stale debug `println("SQL Query:", sqlStr)` from `Search()`

**`chat/bridge.go`**:
- `executeSearchVault`: uses `r.NotesText` (not `r.NotesMD`) in tool result

### Verification
- `go build .` → clean ✓
- `npx tsc --noEmit` → clean ✓

---

## TASK-20260222-014 — Chat: expanded tools (DONE)

### Implementation

**`store/models.go`**:
- Added `TaxonomyItem struct {Name string; Count int}`
- Added `VaultBySection struct {Now, Soon, Anytime int}`
- Added `VaultStats struct` (Total, Open, Completed, Archived, Overdue, Inbox, Notes, Todos, BySection)

**`store/store.go`**:
- Added `GetStats(ctx) (*VaultStats, error)` — single aggregate SQL query with conditional counts
- Added `GetTaxonomyWithCounts(ctx) (projects, contexts, tags []TaxonomyItem, err error)` — three LEFT JOIN queries

**`chat/models.go`**:
- Extended `BridgeStore` interface with `GetTaxonomyWithCounts` and `GetStats`

**`chat/bridge.go`**:
- Renamed `searchVaultTool()` → `buildTools()` returning `[]toolDef`
- Added tools: `get_item` (fetch full item by ID), `list_taxonomy` (projects/contexts/tags with counts), `get_vault_stats` (aggregate counts)
- Added `executeGetItem`, `executeListTaxonomy`, `executeGetVaultStats`
- `executeTool` signature extended with `caps VaultCaps` parameter
- `Send()` uses `buildTools()`, passes `caps` to `executeTool`

### Verification
- `go build .` → clean ✓
- `npx tsc --noEmit` → clean ✓
