---
iteration: 10
date: 2026-02-22
milestone: E1-S3 — Sprint 3 (partial)
tasks_completed: [TASK-20260222-018]
tasks_created: [TASK-20260222-018]
---

# Iteration 10 Run Log

## Objective
Sprint 3, TASK-018: Template storage — `templates` table in `chat.db`, CRUD methods, Wails bindings, built-in seed templates.

---

## TASK-20260222-018 — Templates: storage (DONE)

### Implementation

**`chat/models.go`**:
- Added `Template struct` with fields: `ID`, `Slug`, `Name`, `Description`, `Type`, `Prompt`, `Parameters` (JSON array string), `OutputFormat`, `CreatedAt`, `UpdatedAt`

**`chat/store.go`**:
- Added `templates` table to `chatSchema`:
  - `slug TEXT NOT NULL UNIQUE` — machine-readable key for tool use
  - `type` (generation | query | analysis), `parameters` (JSON array), `output_format`
- Added `ChatStore.CreateTemplate(ctx, *Template) (*Template, error)`
- Added `ChatStore.GetTemplate(ctx, slug) (*Template, error)` — lookup by slug
- Added `ChatStore.ListTemplates(ctx) ([]Template, error)` — all, ordered by name
- Added `ChatStore.UpdateTemplate(ctx, *Template) error` — update by slug
- Added `ChatStore.DeleteTemplate(ctx, slug) error`
- Added internal helpers: `getTemplateByID`, `scanTemplate`, `scanTemplateRow` (via `templateScanner` interface)
- Added `seedBuiltinTemplates(ctx)` — called from `Open()` after schema init; uses `INSERT OR IGNORE` for idempotency
- Seed templates:
  - `weekly-review`: 3 params (`vault_name`, `week_start`, `week_end`); calls get_vault_stats + search_vault (completed + overdue); produces 5-section review
  - `vault-audit`: 1 param (`vault_name`); calls get_vault_stats + list_taxonomy + search_vault (overdue + now); produces 5-section audit report

**`app.go`**:
- Added `ListChatTemplates() ([]chat.Template, error)` — nil → empty slice guard
- Added `SaveChatTemplate(t chat.Template) (*chat.Template, error)` — upsert: checks existing by slug; defaults Parameters/OutputFormat/Type if blank
- Added `DeleteChatTemplate(slug string) error`

**`frontend/wailsjs/go/models.ts`** (auto-generated):
- `chat.Template` class generated with all fields

**`frontend/wailsjs/go/main/App.d.ts`** (auto-generated):
- `ListChatTemplates(): Promise<Array<chat.Template>>`
- `SaveChatTemplate(arg1: chat.Template): Promise<chat.Template>`
- `DeleteChatTemplate(arg1: string): Promise<void>`

### Verification
- `go build .` → clean ✓
- `wails generate module` → clean ✓
- `npx tsc --noEmit` → clean ✓
