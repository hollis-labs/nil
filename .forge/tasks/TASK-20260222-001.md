---
id: TASK-20260222-001
title: "Chat DB schema + migration (v5)"
status: todo
priority: A
project: nanite/chat-addon
milestone: M0
created_at: 2026-02-22
updated_at: 2026-02-22
---

## Description
Add the chat addon tables to `store/schema.sql` (for fresh installs) and add a schema migration v5 to `store/store.go` (for existing users). Tables: `chat_sessions`, `chat_messages`, `action_proposals`, `action_audit`.

## Paths
- `store/schema.sql`
- `store/store.go` (migration slice + `currentSchemaVersion`)

## Acceptance Criteria
- [ ] `chat_sessions`, `chat_messages`, `action_proposals`, `action_audit` tables added to `schema.sql` with `IF NOT EXISTS`
- [ ] Migration v5 entry added; `currentSchemaVersion` incremented to 5
- [ ] Migration is idempotent (`IF NOT EXISTS` guards or catches "already exists")
- [ ] `go build .` passes

## Verification
```bash
go build .
# Manually inspect: open a fresh/existing DB with sqlite3 and verify tables exist
```

## Notes
- See spec-lite.md for full CREATE TABLE statements
- chat.db is a **separate DB** in config dir. However, for MVP the chat tables will live in the **vault DB** to simplify the implementation. This decision can be revisited in a later iteration if vault pollution becomes a concern. The audit log should be sufficient for separation.
- Actually: per spec, chat.db is separate. Do NOT add these tables to store/schema.sql. Instead, create a new `chat/store.go` that opens its own SQLite file at `<configDir>/chat.db`. Update this task to reflect that.

**Revised scope**: Create `chat/` package with a `ChatStore` type that opens `<configDir>/chat.db` and initializes its own schema. No changes to `store/schema.sql` needed.

## Updates
- 2026-02-22: Created (Inception iteration 1)
