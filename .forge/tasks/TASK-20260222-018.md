---
id: TASK-20260222-018
title: "Templates: storage (chat.db table + CRUD methods)"
status: done
priority: B
epic: EPIC-E1
sprint: 3
project: nanite/chat-addon
created_at: 2026-02-22
updated_at: 2026-02-22
blocked_by: []
---

## Description
Add `templates` table to `chat.db` and implement CRUD store methods and Wails-bound backend methods. Foundation for all template features.

See ADR-005 for schema and design decisions.

## Acceptance Criteria
- [x] Schema added to `chatSchema` constant in `chat/store.go`
- [x] `Template` struct added to `chat/models.go`
- [x] `ChatStore.CreateTemplate()`, `GetTemplate(slug)`, `ListTemplates()`, `UpdateTemplate()`, `DeleteTemplate()` methods
- [x] Wails-bound: `ListChatTemplates()`, `SaveChatTemplate()`, `DeleteChatTemplate()`
- [x] Wails TypeScript bindings regenerated (`wails generate module`)
- [x] Seed: 2 built-in templates on first run via `INSERT OR IGNORE`: "weekly-review" and "vault-audit"
- [x] `go build .` + `npx tsc --noEmit` clean

## Updates
- 2026-02-22: Created and completed (Iteration 10). Template struct in models.go. templates table in chatSchema. CRUD methods (Create/Get/List/Update/Delete) + seedBuiltinTemplates (INSERT OR IGNORE). Wails methods ListChatTemplates/SaveChatTemplate/DeleteChatTemplate in app.go. wails generate module run. go build + tsc clean.
