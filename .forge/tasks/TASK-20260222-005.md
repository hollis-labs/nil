---
id: TASK-20260222-005
title: "Wails bindings: expose chat + action methods to frontend"
status: todo
priority: A
project: nanite/chat-addon
milestone: M0
created_at: 2026-02-22
updated_at: 2026-02-22
blocked_by: [TASK-20260222-004]
---

## Description
Add all chat-related Wails-bound methods to `app.go` and regenerate TypeScript bindings. Also add `ChatConfig` to `config/config.go` and wire it up.

## Paths
- `app.go` (add chat fields + methods)
- `config/config.go` (add `ChatConfig`, `VaultCap`)
- `frontend/wailsjs/` (regenerated — do not hand-edit)

## Acceptance Criteria
- [ ] `config.ChatConfig` and `config.VaultCap` structs added to config/config.go
- [ ] `App` struct gets `chatStore *chat.ChatStore` and `chatBridge *chat.Bridge` fields
- [ ] `app.go` startup initializes `chatStore` and `chatBridge` from config
- [ ] Methods added: `StartChatSession`, `EndChatSession`, `SendChatMessage`, `ApproveChatAction`, `DenyChatAction`, `GetChatHistory`, `GetActionAudit`, `GetChatConfig`, `SetChatConfig`
- [ ] `wails generate module` run; `frontend/wailsjs/go/main/App.d.ts` and `App.js` updated
- [ ] `go build .` passes

## Verification
```bash
go build .
wails generate module
# Check frontend/wailsjs/go/main/App.d.ts for new method signatures
```

## Notes
- `SendChatMessage` should: (1) store user message, (2) run FTS5 search if query looks like a search, (3) call `Bridge.Send`, (4) store assistant message, (5) if action present → call `ActionRunner.Propose`, (6) return `ChatResponse` with optional `ProposalID`
- `ApproveChatAction` delegates to `ActionRunner.Approve` with the vault store resolved from `vaultMgr`

## Updates
- 2026-02-22: Created (Inception iteration 1)
