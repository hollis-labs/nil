---
id: TASK-20260222-002
title: "Go: ChatMessage model + Store methods"
status: todo
priority: A
project: nanite/chat-addon
milestone: M0
created_at: 2026-02-22
updated_at: 2026-02-22
blocked_by: [TASK-20260222-001]
---

## Description
Create `chat/models.go` with Go structs for `ChatSession`, `ChatMessage`, `ActionProposal`, `AuditEntry`, `ChatResponse`, `ActionResult`. Create `chat/store.go` with `ChatStore` type and CRUD methods.

## Paths
- `chat/models.go` (new)
- `chat/store.go` (new)

## Acceptance Criteria
- [ ] `ChatSession`, `ChatMessage`, `ActionProposal`, `AuditEntry`, `ChatResponse`, `ActionResult` structs defined with correct json tags
- [ ] `ChatStore.CreateSession`, `AddMessage`, `CreateProposal`, `UpdateProposalStatus`, `AppendAudit` methods implemented
- [ ] `ChatStore.GetMessages(sessionID)`, `GetProposal(id)`, `GetAudit(limit)` implemented
- [ ] `go build .` passes

## Verification
```bash
go build .
```

## Notes
- Use `modernc.org/sqlite` (same driver as main store, no CGo)
- DB opens at `<configDir>/chat.db`
- All methods take `context.Context` as first arg (consistent with store/store.go pattern)

## Updates
- 2026-02-22: Created (Inception iteration 1)
