---
id: TASK-20260222-019
title: "Templates: list_templates + use_template tools in Bridge"
status: done
priority: B
epic: EPIC-E1
sprint: 3
project: nanite/chat-addon
created_at: 2026-02-22
updated_at: 2026-02-22
blocked_by: [TASK-20260222-018]
---

## Description
Register `list_templates` and `use_template` tools in the Bridge. Claude can discover available templates and invoke them with parameter substitution.

## Acceptance Criteria
- [x] `TemplateStore` interface in `chat/models.go` with `ListTemplates` + `GetTemplate`
- [x] `Templates TemplateStore` field added to `BridgeRequest`
- [x] `list_templates` tool: calls store, returns `{templates: [{slug, name, description, type, parameters}], count}`
- [x] `use_template` tool: fetches template, applies `strings.ReplaceAll` for each `{{param}}`, returns `{slug, name, rendered_prompt}`
- [x] Missing params: left as-is in rendered string (Claude fills from context)
- [x] System prompt: template guidance bullet added to search/read section
- [x] Both tools added to `buildTools()` alongside existing tools
- [x] `executeTool` dispatches template tools before vault-store nil check (templates don't need vault store)
- [x] `app.go`: `Templates: a.chatStore` wired in `SendChatMessage`
- [x] `go build .` + `npx tsc --noEmit` clean

## Updates
- 2026-02-22: Created and completed (Iteration 11). TemplateStore interface in models.go. BridgeRequest.Templates field. list_templates + use_template in buildTools(). executeListTemplates + executeUseTemplate. executeTool dispatches template tools before vault nil check. System prompt updated. app.go wired. go build + tsc clean.
