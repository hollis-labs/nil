---
id: TASK-20260222-015
title: "Chat: vault profile in system prompt"
status: done
priority: B
epic: EPIC-E1
sprint: 2
project: nanite/chat-addon
created_at: 2026-02-22
updated_at: 2026-02-22
blocked_by: [TASK-20260222-014]
---

## Description
Inject a live vault snapshot into the system prompt so Claude has immediate context about the vault (item counts, overdue, inbox, projects, contexts) without needing to call `get_vault_stats` or `list_taxonomy` first.

## Paths
- `chat/profile.go` — new file: `ComputeVaultProfile(ctx, BridgeStore) string`
- `chat/bridge.go` — `buildSystemPrompt` accepts `vaultProfile string`; `{vault_profile}` placeholder substituted in template; `Send()` calls `ComputeVaultProfile` before building the prompt

## Acceptance Criteria
- [x] `ComputeVaultProfile` calls `GetStats` + `GetTaxonomyWithCounts`; returns empty string on error
- [x] System prompt template has `{vault_profile}` placeholder
- [x] When profile non-empty: injected with preceding newline
- [x] When profile empty: `\n{vault_profile}` removed cleanly (no blank line)
- [x] `go build .` + `npx tsc --noEmit` clean

## Updates
- 2026-02-22: Created and completed (Iteration 8). New chat/profile.go with ComputeVaultProfile. buildSystemPrompt signature extended. {vault_profile} substitution in Send(). go build + tsc clean.
